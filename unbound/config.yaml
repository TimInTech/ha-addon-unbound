name: "Unbound"
version: "1.1.1"
slug: "unbound"
description: "Recursive, DNSSEC-validating DNS resolver on 127.0.0.1:5335, e.g. as upstream for Pi-hole"
url: "https://github.com/TimInTech/ha-addon-unbound"
arch:
  - amd64
  - aarch64
startup: system
init: false
host_network: true
